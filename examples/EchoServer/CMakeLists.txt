add_executable(EchoServer main.cpp)
target_link_libraries(EchoServer PRIVATE mcp-server)
if(CMAKE_CXX_COMPILER_ID MATCHES "Clang" AND WIN32)
    target_link_options(EchoServer PRIVATE "LINKER:/LTCG")
endif()
