# 更新摘要

- 2026-09-15 — 知识库全量一致性审计：22 页源码对照修正（错误码 19→20 补 SessionExpired、序列化 87→88 对、Notifications 4→5 类、JsonFields 118→119、`Get*` 7→6 组、全库行号校正；Auto `-32022` 三分支按代码判据重写；Streamable HTTP 版本头只认 initialize POST 响应、GET 流承载请求、SSE 空 data 静默与不解析 `retry:`；并发页客户端线程模型重写、WebSocket Close 不回调会话；build 的 `-march=native`/OpenSSL/docs.yml 修正、OAuth 补 client_credentials 入口）；唯一死链修复；数值复核 ctest -N 578 / 断言 1836 / 17 目标逐项吻合；AGENTS.md 三处修正（554→578 用例、`MCP_RUN_WITH_TIMEOUT` + `MarkAbandoned`、PKCE 随机源），见 [/changelog/2026-09-15-log.md](/changelog/2026-09-15-log.md)
- 2026-09-15 — 发布准备：版本号更新至 0.3.4——`kSdkVersion` 0.3.3→0.3.4（唯一来源，`CMakeLists.txt` 自动提取为 `PROJECT_VERSION`），同步 docs 中英 `client/overview.md` 默认 `client_info` 与 [wiki/classes/mcp-client.md](/classes/mcp-client.md) 默认值（0.3.1→0.3.4）；docs 全量扫描无其他 SDK 版本引用需改（协议版本与历史 changelog 未动），SSE keepalive 描述与 `KeepAliveLoop` 实现一致未改，见 [/changelog/2026-09-15-log.md](/changelog/2026-09-15-log.md)
- 2026-09-15 — conformance client sse-retry 修复：`SleepInterruptibly` 谓词 `!stop` → `running`（恢复丢失提交 8363d75 的退避语义：原运行中退避从不生效、Close 后反而睡满）；`ParseSseBlock` 修复 `data:` 后仅空白产出 `" "` 致反序列化失败报 `HTTP SSE block parse failed`（referee sse-retry priming 事件 `id/retry/data:` 命中），现空串由 `DispatchSseBlock` 静默忽略；client 基线恢复 `sse-retry` 条目（36 → 37 条，归因 retry 等待缺失 -70ms 与 Last-Event-ID WARNING，注明删除条件）；Node 24 + referee 0.2.0-alpha.11 实测 client leg exit 0、server leg 65 passed / 2 failed 均在基线、全量 ctest 578/578 通过，见 [/changelog/2026-09-15-log.md](/changelog/2026-09-15-log.md)
- 2026-09-15 — CI 与 conformance 修复：服务端 URL elicitation 注册先于发送（修复 complete 通知先到被静默丢弃的竞态；`SendRequest` 异常清理条目后 rethrow；超时消息区分 `(no response)`/`(no complete notification)`，未知 complete id 记 Warning）；客户端先发 complete 通知再提交响应（发送失败仅记 Error）；`KeepAliveLoop` 谓词恢复为仅 `!running_`（修复有 SSE 客户端即忙循环烧 CPU，恢复未进入分支历史的 f5c7262）；conformance 双 job `node-version: 22`（referee 依赖 Node 22+ `fs.globSync`）、server 就绪探测改 POST `initialize`（GET 会建 SSE 长流致 `--max-time` 超时）；`UrlElicitationRoundTrip` repeat 100 零失败、全量 ctest 578/578 通过，见 [/changelog/2026-09-15-log.md](/changelog/2026-09-15-log.md)
- 2026-09-14 — 自研测试框架 P0+P1 增强：致命断言（`ASSERT_*`/`FAIL` 置 fatal 标志 + `HasFatalFailure`/`NO_FATAL_FAILURE`）、`SCOPED_TRACE` 调用链定位、`ToString` 优先级 ADL `PrintTo`→`operator<<`→容器展开、扩展断言与 `Eq`/`HasSubstr`/`SizeIs`/`ElementsAre` 匹配器；运行控制（`--gtest_filter` 多模式/取反、`--gtest_list_tests`、`--gtest_repeat`、`--gtest_shuffle`/`--gtest_random_seed`、`--gtest_break_on_failure`、`--gtest_output=json:`）；生命周期（`GTEST_SKIP`、`SetUpTestSuite`/`TearDownTestSuite` 钩子、`Environment`、`RecordProperty`/`AddTestEndListener`）；`MCP_RUN_WITH_TIMEOUT` 统一超时护栏（超时记失败 + `MarkAbandoned`，ctest `TIMEOUT 120` 兜底）；`FakeTransport` 测试替身；全量 17 目标 / 578 用例（554→578，578/578 通过），见 [/changelog/2026-09-14-log.md](/changelog/2026-09-14-log.md)
- 2026-09-12 — 期三生产就绪（Bearer 鉴权 + 官方 conformance + 存储参考实现）：服务端 Bearer 鉴权（`bearer_auth` 配置、401 invalid_token / 403 insufficient_scope 挑战、`/.well-known/oauth-protected-resource` 匿名端点、未配置零回归、客户端 auth_challenge_handler 闭环）；官方 conformance suite 接入（`MCP_BUILD_CONFORMANCE`、conformance-server/client fixture、`conformance.yml` 双 job pin referee 0.2.0-alpha.11、`baseline.yaml` 归因基线 server 3 条 / client 36 条、server leg 65 passed / 2 failed 均在基线）；`FileEventStore`（JSONL + 跨进程文件锁 + trim）与 `EventStore` 虚化、`SessionStore` 抽象与 stateful 外部会话接管（未知 id -32009）；生产修复（SSE 头重复、`SendLoggingMessage` level 字符串、`CompleteRequestParams` 官方形状、`SerializeEmptyResult` 去 resultType、TcpSocket fd_ atomic 竞态、NOMINMAX 全局）；`request_state_key` HMAC 签发、`PromptOptions::arguments`、`ElicitResult::content` 成员改名；sccache-action v0.0.11；全量 17 目标 / 554 用例，见 [/changelog/2026-09-12-log.md](/changelog/2026-09-12-log.md)
- 2026-09-11 — 期二收尾（tasks 自动化 + URL elicitation + 传输自愈）：`RequireInitialized` era 门控（modern era 放行未初始化）；任务化 tools/call 立即返回 `CreateTaskResult` + `notifications/tasks/status` 流转 + `RequestContext::IsCancellationRequested` 协作取消（tasks/* 沿用 2025 era gate）；客户端 `CallToolAsTask`/`GetTask`/`PollTaskToCompletion`/`CancelTask` 完整流；URL elicitation（`ElicitUrl` + `SetUrlElicitationHandler` + 自动 complete 通知，`ElicitResult` 键修正 `values`→`content`）；404 会话自愈（`SessionExpired=-32009`、`reinit_on_expired_session` 重协商重放恰一次）；`max_total_timeout` 总量封顶（progress 续命不可越过）；`*All` 翻页聚合 + `ListPages` 不收敛抛错修复；传输层修复（Notification/Response 独立 POST、DoPost 边读边分发、WinHttp 增量读、discover serverInfo 容缺与版本交集）；TS conformance 双向联调（modern era Auto 走通、legacy form elicitation 互通）；全量 521 用例，见 [/changelog/2026-09-11-log.md](/changelog/2026-09-11-log.md)

