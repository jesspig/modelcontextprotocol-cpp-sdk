# 更新摘要

- 2026-08-15 — 知识库一致性审计（对照源码逐条核实 27 个页面）：修正 CI job 数（6→**8**）、tasks/* 独有通知数（5→**6**）、McpTypes 序列化对数（84→**87**）与 Results 计数（19→**20**）；纠正 `RegisterTool` 唯一同名覆盖（资源/提示词为 vector 追加）、`CacheIfHinted` 嵌套优先（与 `ExtractCacheHint` 方向相反）、`transport_mode` 无运行时读取点（AutoDetect 回落未实现）、`CreateTask` 不返回 bool、临时文件名为 `.tmp.<pid>.<counter>`、PKCE 无 OpenSSL 时 Win32 走 `BCryptGenRandom`；`-march=native` 补充非 Apple；docs.yml 触发分支为 `develop`；同步 13 处过时行号引用与 Streamable HTTP 客户端线程模型（Win32 SSE 读线程已移除），见 [/changelog/2026-08-15-log.md](/changelog/2026-08-15-log.md)
- 2026-08-15 — 跨平台加固与 CI 现代化：SIGPIPE 统一（`SO_NOSIGPIPE` + 进程级忽略）、Posix `Read` ECONNRESET 视为 EOF、fork 前移 argv/envp/PATH 解析 + 管道 `FD_CLOEXEC`、Win32 stdio 管道 OVERLAPPED 化（NamedPipe 64KB）与 `WriteSync` 锁外化、Sha1 无 OpenSSL 回退 `BCryptGenRandom`、stdio `ReadLoop` `shared_from_this()` 保活；核心库浮点 `to_chars` 序列化（locale 无关）/下溢归零/错误码枚举校验/progress 与 maxTokens 类型校验；HttpClient IPv6 Host 加括号、TLS close_notify 收尾（500ms 等待）、Win32 SSE 8MB 上限、HttpServer 连接对象化（消除 double-close）、SSE 客户端 Last-Event-ID 保留 + 5 分钟空闲超时；CI 删除 Setup Ninja、sccache-action 统一三平台、actions 升 v5/v6、docs 分支 develop，见 [/changelog/2026-08-15-log.md](/changelog/2026-08-15-log.md)
- 2026-08-15 — 性能优化与响应缓存增强：JsonParser 数组预分配（前向扫描预算元素数）+ 字符串分段批量复制；Dump 改单缓冲（`std::string` + `EstimateSize` 预 reserve + `snprintf`）；ResponseCache 重构——cursor/URI 上下文键、TTL 钳制 24h、public/private 双分区（Close 清 private）、`resources/updated` 单键失效、`ReadResource` 支持 `cache_mode`/`max_age_ms`；MRTR `input_requests` 三类型（sampling/roots）分派 + state-only 退避（50ms ×2 封顶 250ms），见 [/changelog/2026-08-15-log.md](/changelog/2026-08-15-log.md)
- 2026-08-15 — 服务端协议一致性补全：`HandleDiscover` 设置协商版本（配置值或 kLatest）+ supported_versions 全表 5 个；`RegisterTool` 工具名校验 `^[A-Za-z0-9._-]{1,128}$`；tasks 状态通知（`SendTaskStatus` 公开方法、update/cancel 完成发通知，WireCodec 2025-only 通知集合加回 tasks/* 系列（当前 6 个），共 17 种）；`ITransport::Start` 默认空实现 + McpServer 构造自动启动（stdio 免手动 Start）；HttpClient 响应体读取超时前移修复，见 [/changelog/2026-08-15-log.md](/changelog/2026-08-15-log.md)
- 2026-08-15 — 移除 benchmark：删除 `tests/benchmark/`（`mcp-json-bench`/`mcp-http-bench` 二进制、baseline、驱动脚本）与 `.github/workflows/benchmark.yml` 性能回归门禁，同步清理 CMake 目标注册，见 [/changelog/2026-08-15-log.md](/changelog/2026-08-15-log.md)
- 2026-08-15 — 与官方 5 语言一致性收尾（P07/P08 客户端侧）：客户端会话头接线（known_session_id 携带、响应 `Mcp-Session-Id` 捕获复用、Close 时仅持有会话 id 才发 DELETE）、SubscribeAsync 等待 acknowledged 首帧（5s 超时对齐 ts，请求携带 `_meta` subscriptionId，不覆盖用户通知处理器）、NotifySubscribers 事件通知 subscriptionId 优先回显 `session_id`（回退自增 id）；测试新增 6 例（含超时路径），见 [/changelog/2026-08-15-log.md](/changelog/2026-08-15-log.md)
- 2026-08-15 — HTTP 请求响应路径规范化（P06a/b/c）：stateless 默认开启；请求统一同步等待响应 → 200 + SSE 流（写完即关连接），协议错误映射 400/404（-32020/-32021/-32022/-32600/-32602/-32700 → 400、-32601 → 404），202 仅通知；客户端忽略 202、4xx body 解析 JSON-RPC error、POST SSE 同步读（Win32 移除 SSE 线程）；测试 6 例新增，见 [/changelog/2026-08-15-log.md](/changelog/2026-08-15-log.md)
