# 更新摘要

- 2026-09-20 — 版本号更新至 0.3.5：`include/mcp/McpVersion.hpp` 的 `kSdkVersion` `0.3.4` → `0.3.5`（唯一来源，`CMakeLists.txt` 自动提取，无需改 CMake；`client_info`/`server_version`/`server_info` 默认值随之更新）；在线文档中英 `client/overview.md` 与知识库 `classes/mcp-client.md` 默认客户端标识同步，详见 [/changelog/2026-09-20-log.md](/changelog/2026-09-20-log.md)

- 2026-09-20 — Streamable HTTP 服务端请求有界等待监听者：新增 `HttpServer::SseClientCount`（`sse_mutex_` 改 `mutable`），`StreamableHttpServerTransport::SendMessageAsync` 仅对 `IsRequest` 在零监听者时 50ms 步长轮询至多 2s（`Disconnected` 可中断，超时 Warning 后仍广播；Notification 保持 fire-and-forget）；5 文件 35 增 1 改，未碰 conformance 基线与 fixture；背景为 CI sampling 约 30s 超时 / sep1330 1 过 1 挂而本地复现全过（根因 GET 广播快照为空静默丢弃），修复后 66 passed 1 failed（仅基线内 progress）；wiki 3 页同步（streamable-http/http/http-server），详见 [/changelog/2026-09-20-log.md](/changelog/2026-09-20-log.md)

- 2026-09-20 — Windows `GetObject` 宏修复与 `NOGDI`：`cmake/CompilerOptions.cmake` 在 clang-cl/MSVC 两分支 `NOMINMAX` 后追加 `NOGDI`（排除 `wingdi.h` 的 `GetObject` 宏）；`JsonValue.hpp`/`McpParamAnnotations.hpp` 新增头侧 `#ifdef GetObject` 条件 `push_macro`/`undef`/尾部 `pop_macro` 隔离；`HttpServerImpl.cpp` 与 Streamable HTTP 双端三源文件 `push_macro` 移入 `#ifdef` 内并补尾部条件恢复（此前两文件无恢复、一文件 `push` 在 `#ifdef` 外）；6 文件 47 增 2 删，未碰 conformance 基线与 fixture；wiki 5 页同步（清除“仅 NOMINMAX”/“未定义 NOMINMAX 时”过时描述），详见 [/changelog/2026-09-20-log.md](/changelog/2026-09-20-log.md)

- 2026-09-20 — 知识库增量审计与源码同步：按最新实现将测试基线核实为 **18 目标 / 632 用例 / 2154 断言**；补入 SEP-2243 `x-mcp-header` 注解镜像与服务端校验、span 观测钩子、HTTP 8KB 响应头缓冲与 transport benchmark；新增 URI 模板、响应缓存、参数头、span hook 四个概念页；修订方法常量 41、MRTR 兼容时代、存储/OAuth/日志/并发失败语义，并维护 `AGENTS.md` 的 `## 项目知识库` 入口，详见 [/changelog/2026-09-20-log.md](/changelog/2026-09-20-log.md)
- 2026-09-20 — 协议合规补全、重复实现收敛与授权增强：服务端对非 `initialize` 请求按 `_meta.protocolVersion` 回 `-32022`（带 `requested`/`supported`），并新增 `_meta` 子字段类型校验（`invalid _meta field: <键名>` → `InvalidParams`）；在途请求取消标志接线（`GetIncomingCancellationFlag` / `EraseIncomingCancellationFlag` + `notifications/cancelled` 置位 + 派发前注入 `RequestContext`），普通工具 handler 的 `IsCancellationRequested()` 不再恒 false；新增 `McpServer::SendResourceUpdated(uri)`，三个 list_changed 发送点按时代分派（2026 按 filter 过滤、2025 及更早广播）；OAuth 补 RFC 8707 `resource`、授权响应侧 RFC 9207 `iss` 校验与 CIMD；**推翻同日晚间的维持独立结论**——大小写归一 / base64url / SSE 行解析三处重复实现现已分别收敛到 `include/mcp/detail/{StringUtils,Base64Url,SseEventParser}.hpp`；`scripts/run-conformance.sh` 修复 `BIN_DIR` 缺 target 子目录导致 server 模式无法运行的问题；docs 中英 5 页与 wiki 7 页同步，见 [/changelog/2026-09-20-log.md](/changelog/2026-09-20-log.md)
- 2026-09-20 — URI 模板匹配接入、残留清理与测试计数刷新：新增 `src/detail/UriTemplate.hpp`（RFC 6570 `Parse`/`Expand`/`Match`）与新测试目标 `mcp-uri-template-tests`（20 用例）；`HandleReadResource` 改两轮遍历把模板实例 URI 路由到 `template_handler`（静态资源优先），`RegisterResourceTemplate` 对非法模板改抛 `McpError(InvalidParams)`（行为变更）；官方 conformance server leg 的 `resources-templates-read` 转绿并从 conformance 基线删除该条目（基线说明见 [/tests.md](/tests.md)）；删 17 个零调用序列化函数与空壳 `src/core/McpTypesOptions.cpp`、`TrimWhitespace` 并入 `NetIoUtil.hpp`；base64url / SSE 解析器 / 大小写归一三处重复实现当时复核后维持独立（同日晚间已收敛，见上条）；当时测试口径为 18 目标 / 612 用例 / 2048 断言，当前基线见上条，详见 [/changelog/2026-09-20-log.md](/changelog/2026-09-20-log.md)
- 2026-09-19 — 死代码清理与客户端 `Mcp-Param-*` 止血：删 `JsonValue::FromObject`/`FromArray`、`MCP_LOG_TAG` 宏、`McpSessionHandler::RemoveRequestHandler`/`RemoveNotificationHandler` 与 `ServerOptions::validate_tool_input`/`validate_tool_output` 死字段，另清 2 处无用 `<sstream>`；客户端不再生成任何 `Mcp-Param-*` 请求头（旧实现无差别镜像 `params` 顶层标量、从未按规范读取 `inputSchema` 的 `x-mcp-header` 注解，规范要求客户端镜像被注解参数（MUST）仍是未实现的遗留缺口），仅留 `Mcp-Method`/`Mcp-Name`，新增 `StreamableHttpTest.ClientOmitsMcpParamHeaders` 守护；服务端接收/回显与 CORS 允许列表保留。同日晚间 MRTR `inputRequests` 对齐官方 2026-07-28 规范：删除 `InputRequestElicit`/`InputRequestSampling`/`InputRequestRoots` 与固定四字段 `InputRequests`，改为 `InputRequest{method, params}` + `std::map<std::string, InputRequest, std::less<>>`；序列化逐项校验（畸形项抛 `InvalidParams`）；`MakeInputRequestForElicitation` 返回 `InputRequest`、`MakeInputResponseFromElicitResult` 产出裸结果（无 `resultType`/`meta`），新增 sampling/roots 请求与裸响应 helper；客户端 `TryFulfillInputRequired` 按 `method` 分派、`inputResponses` 用服务端原键（未知 method → `MethodNotFound`）；conformance fixture 键名改语义标识符，新增规范样例契约测试与派发用例；docs 中英 mrtr/tools 与 wiki 3 页同步（序列化对数 88→86），见 [/changelog/2026-09-19-log.md](/changelog/2026-09-19-log.md)
