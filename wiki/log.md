# 更新摘要

- 2026-09-11 — 期二收尾（tasks 自动化 + URL elicitation + 传输自愈）：`RequireInitialized` era 门控（modern era 放行未初始化）；任务化 tools/call 立即返回 `CreateTaskResult` + `notifications/tasks/status` 流转 + `RequestContext::IsCancellationRequested` 协作取消（tasks/* 沿用 2025 era gate）；客户端 `CallToolAsTask`/`GetTask`/`PollTaskToCompletion`/`CancelTask` 完整流；URL elicitation（`ElicitUrl` + `SetUrlElicitationHandler` + 自动 complete 通知，`ElicitResult` 键修正 `values`→`content`）；404 会话自愈（`SessionExpired=-32009`、`reinit_on_expired_session` 重协商重放恰一次）；`max_total_timeout` 总量封顶（progress 续命不可越过）；`*All` 翻页聚合 + `ListPages` 不收敛抛错修复；传输层修复（Notification/Response 独立 POST、DoPost 边读边分发、WinHttp 增量读、discover serverInfo 容缺与版本交集）；TS conformance 双向联调（modern era Auto 走通、legacy form elicitation 互通）；全量 521 用例，见 [/changelog/2026-09-11-log.md](/changelog/2026-09-11-log.md)
- 2026-09-11 — 第一期互操作（progress 双向通道 + GET SSE 接收流 + _meta 线上位置修正）：服务端 `SendProgress` 与客户端 `on_progress` 回调（自动 progressToken、RAII 清理、双 era 落点）；`SendRootsListChanged` 版本门控（>= 2025-06-18）；Streamable HTTP 客户端 GET SSE 接收流（enable_listen_stream 默认 true、405 静默放弃、退避重连 5 次、Last-Event-ID）；`_meta` 线上位置改 `params._meta`（JsonRpc 序列化层 + WireCodec + SessionHandler）；MCP-Protocol-Version 头自学习；RequestContext 存值修复悬垂指针；新增 Phase1InteropTests 4 用例、全量 482、TS conformance 联调 7/7，见 [/changelog/2026-09-11-log.md](/changelog/2026-09-11-log.md)
- 2026-08-28 — 代码-文档一致性审计：修正 build.md `docs.yml` 触发分支 `develop`→`master`；修正 mrtr.md 错误引用文件（`McpTypesResults.cpp:395`→`McpClient.cpp:657`）、mcp-client.md/client.md `CacheIfHinted` 引用 `:86`→`:769` 与 Auto 回退 `:248`→`:247`；http-server.md/http.md/streamable-http.md 补充 `bind_host`/`host` 监听地址可配置；同步 9 处过时行号引用（concurrency/version-negotiation/wire-codec/server/mcp-server/mcp-session-handler/client/stdio/http），见 [/changelog/2026-08-28-log.md](/changelog/2026-08-28-log.md)
- 2026-08-28 — bind_host 支持 IPv6 监听地址：HttpServerImpl::Start 按字面量自动选族（先 inet_pton AF_INET6 再 AF_INET），支持 IPv4/IPv6 字面量（含可选 [] 包围）；空 bind_host 仍仅 INADDR_ANY（向后兼容）；新增 BindHostIpv6LoopbackAccepts / BindHostInvalidIpv6Throws 用例，见 [/changelog/2026-08-28-log.md](/changelog/2026-08-28-log.md)
- 2026-08-28 — 可观测性增强：全局日志钩子（SetLogHandler/SetLogLevel/LogRecord）、HttpServer bind_host 可配置、binding/Host 拒绝诊断日志；测试 LogTests + HttpServer bind_host 用例，见 [/changelog/2026-08-28-log.md](/changelog/2026-08-28-log.md)
- 2026-08-15 — 知识库一致性审计（对照源码逐条核实 27 个页面）：修正 CI job 数（6→**8**）、tasks/* 独有通知数（5→**6**）、McpTypes 序列化对数（84→**87**）与 Results 计数（19→**20**）；纠正 `RegisterTool` 唯一同名覆盖（资源/提示词为 vector 追加）、`CacheIfHinted` 嵌套优先（与 `ExtractCacheHint` 方向相反）、`transport_mode` 无运行时读取点（AutoDetect 回落未实现）、`CreateTask` 不返回 bool、临时文件名为 `.tmp.<pid>.<counter>`、PKCE 无 OpenSSL 时 Win32 走 `BCryptGenRandom`；`-march=native` 补充非 Apple；docs.yml 触发分支为 `develop`；同步 13 处过时行号引用与 Streamable HTTP 客户端线程模型（Win32 SSE 读线程已移除），见 [/changelog/2026-08-15-log.md](/changelog/2026-08-15-log.md)
- 2026-08-15 — 跨平台加固与 CI 现代化：SIGPIPE 统一（`SO_NOSIGPIPE` + 进程级忽略）、Posix `Read` ECONNRESET 视为 EOF、fork 前移 argv/envp/PATH 解析 + 管道 `FD_CLOEXEC`、Win32 stdio 管道 OVERLAPPED 化（NamedPipe 64KB）与 `WriteSync` 锁外化、Sha1 无 OpenSSL 回退 `BCryptGenRandom`、stdio `ReadLoop` `shared_from_this()` 保活；核心库浮点 `to_chars` 序列化（locale 无关）/下溢归零/错误码枚举校验/progress 与 maxTokens 类型校验；HttpClient IPv6 Host 加括号、TLS close_notify 收尾（500ms 等待）、Win32 SSE 8MB 上限、HttpServer 连接对象化（消除 double-close）、SSE 客户端 Last-Event-ID 保留 + 5 分钟空闲超时；CI 删除 Setup Ninja、sccache-action 统一三平台、actions 升 v5/v6、docs 分支 develop，见 [/changelog/2026-08-15-log.md](/changelog/2026-08-15-log.md)
