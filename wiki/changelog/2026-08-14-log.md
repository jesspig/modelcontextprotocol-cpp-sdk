# 2026-08-14 更新日志

## 2026-08-14-23 协议错误码与版本表对齐 5 语言（P01/P02a/P10/E10）

- **P01 错误码 data 形状对齐**：`ErrorCodes.hpp` 新增 MCP 专用码 `ResourceNotFound = -32002`（对齐 python/ts/rust/go/csharp）；内部反序列化码 `DeserializeFailed` 移至 `-32008`（避开 -32003 ConnectionRefused/-32004 TlsHandshakeFailed，枚举与 `message()`/`default_error_condition()` 同步，`ResourceNotFound` 映射 `errc::no_such_file_or_directory`）；所有 `throw McpError(DeserializeFailed)` 调用点经枚举名自动跟随新值，无需改源码
- **P01 2026 错误重映射取消**：`Rev2026Codec::EncodeErrorCode` 不再把内部错误伪装为协议码（RequestTimeout/ConnectionRefused/TlsHandshakeFailed 记日志并映射 `InternalError(-32603)`）；`-32021 requiredCapabilities` data 由 `McpSessionHandler::VerifyCapability` 构造（形状保持），`-32022 requested/supported` data 在客户端协商路径消费——WireCodec 只移除映射
- **P02a 版本表对齐**：`kProtocolVersions[]` 移除 "2024-10-07"，保留 5 个（2024-11-05 起）；唯一遍历点 `McpServer.cpp:1059` 不依赖索引，无需适配；`IsModernProtocolVersion` 判定不变
- **P10 缺版本默认值**：新增 `kDefaultNegotiatedProtocolVersion = "2025-03-26"`（对齐 5 语言 `DEFAULT_NEGOTIATED_PROTOCOL_VERSION`）；`McpServer::HandleInitialize` 未命中支持表或缺失版本时回退该值（原 `kLegacyProtocolVersion` 2025-11-25），客户端显式声明路径（`HandshakeInitialize`）不变
- **E10 meta 构造**：`StampOutgoingRequest` 显式移动序列化结果、先写可选键后写 protocolVersion，输出 JSON 不变，无全局状态
- 测试更新：ProtocolConformance 错误码断言分组（MCP 专用 8 + 内部 6）、`WireCodec2026ErrorRemapping` 改 -32603、新增协议码透传/2025 恒等/`kProtocolVersions` 内容/`kDefaultNegotiatedProtocolVersion` 4 组断言；McpServerTests 新增 2 例（声明不支持版本、缺失版本均回退 2025-03-26）
- 文档同步：AGENTS.md、wiki/modules/core.md、wiki/classes/wire-codec.md、wiki/classes/mcp-server.md、wiki/concepts/version-negotiation.md、docs/zh|en/advanced/protocol-versions.md

## 2026-08-14-23 传输层行为与官方 5 语言实现一致性对齐（P04/P12/P15/P17/P18）

- **P04 客户端版本头跨平台统一**：`StreamableHttpClientTransport.cpp` POSIX 分支 `DoPost` 补 `MCP-Protocol-Version: 2026-07-28` 请求头，与 Win32 分支行为一致（transport 层无协商状态，固定发最新版本，覆盖 initialize/discover 阶段）；Mcp-Method/Mcp-Param-*/Mcp-Name 提取逻辑两分支结构一致，解析失败回退值差异（Win32 `tools/call` vs POSIX `unknown`）为既有行为保留
- **P15 SSE 重连退避对齐 go/ts/python 共识**：`SseClientTransport.cpp` 退避改 1s 起始 ×1.5 增长、封顶 30s（`kBackoffCap`）、最多 2 次重连尝试（`kMaxReconnectAttempts`，原 5 步 2 的幂退避）；新增 `retry:` 字段最小解析（正整数毫秒，非法忽略），收到后优先以该值等待（覆盖退避）
- **P17 子进程终止等待 5000ms→2000ms**：`StdioClientTransport.cpp` `Close()` 中 `process_->Terminate(2000)`（对齐 python/ts）
- **P18 默认请求超时 30s→60s**：`McpSession.hpp` `kDefaultRequestTimeout{30000}→{60000}`（对齐 ts/csharp）；`McpClient.cpp` 引用注释同步；`ClientOptions.discover_probe_timeout=5s` 与 csharp 一致，确认不改
- **P12 HTTP 请求体限制 8MB→4MiB**：`Limits.hpp` 新增 `kMaxHttpBodyBytes = 4MiB`；`HttpServerImpl.cpp` 的 `kMaxBodyBytes` 与 `StreamableHttpServerTransport.cpp` 的 413 检查改引该常量；stdio/SSE/响应体的 `kMaxMessageSize`（8MB）保持不变
- 测试核查：`kDefaultRequestTimeout`/`kMaxBodyBytes`/Terminate 超时/重连退避均无测试直接断言旧值，无需改测试
- wiki 同步：sse/stdio/streamable-http/protocol/mcp-session-handler/transport 6 页

## 2026-08-14-21 0.3.0 发布前全面文档核查（wiki 29 页 + docs 中英 43 页）

- 代码版本默认值更新为 0.3.0：`CMakeLists.txt` project VERSION 0.1.0→0.3.0、`ClientOptions.hpp` 默认 `client_info {"mcp-cpp-client","0.3.0"}`、`StreamableHttpServerTransport.hpp` `server_version`、`McpServer.cpp` 默认 `server_info {"mcp-server","0.3.0"}`（默认值即 SDK 版本，无测试依赖该值）
- 3 个子代理并行核查（A：wiki 全量 29 页；B：docs/zh 22 页；C：docs/en 21 页，以 zh 镜像为准），主代理复核全部改动
- **wiki 修正**：
  - tests.md/index.md：目标数 15→**16**（tests/unit 实测 **13** 目标，含 mcp-net-tests；此前 12/15 为统计口径错误，AGENTS.md 同步修正）；断言数 908→912（core-tests 165→166、SelfTests 27→30，静态统计复核一致）
  - classes/mcp-client.md：`client_info` 默认 0.1.0→0.3.0
  - classes/http-server.md：SSE 写回调捕获连接对象行号 389→324（`make_shared<TcpSocket>` 实际位置，389 为 OPTIONS 204 处理）
  - concepts/oauth.md：`VerifyResourceMatch` 行号 121→125（125 为函数定义，121 为 RFC 注释）
  - transports/sse.md：`event: message` 超限行为改为「`NotifyError` 并丢弃该事件」（非静默丢弃）
  - 其余 23 页内容未变：timestamp 保持原值（初稿曾统一改写 timestamp，已全部还原；timestamp 仅在内容变更时更新）
- **docs/zh 修正（7 页）**：
  - index.md：测试数量 278→392（含 111 一致性）
  - guide/installation.md：OpenSSL「必需硬依赖」→**可选依赖**（`find_package(OpenSSL QUIET)`、未找到时 TLS 禁用 + PKCE 回退内置 SHA-256）；补「configure 无需联网」
  - guide/transports.md：`server_version` 默认 0.1.0→0.3.0
  - client/overview.md：`client_info` 默认 0.3.0；移除 `SetSamplingHandler`/`SetRootsHandler` 废弃 API 示例（SEP-2577），改一行弃用说明
  - client/oauth.md：OpenSSL 可选依赖；API 表补 `Authenticate`/`RefreshTokens`/`StepUpAuthorization`/`AuthenticateClientCredentials` 的 `bool` 返回；PKCE 回退补 `BCryptGenRandom`（Windows）/`std::random_device`（POSIX）分支（与代码 OAuthClientProvider.cpp:168-189 一致）
  - advanced/protocol-versions.md：错误码重映射表修正（-32001→HeaderMismatch 实际为 -32020 等三组映射，WireCodec.cpp:229-235）
  - examples/echo-server.md：`std::toupper` 补 `static_cast<unsigned char>`（与 examples/EchoServer/main.cpp:79 对齐）
- **docs/en 镜像同步（5 页）**：index/installation/transports/client-overview/client-oauth 与 zh 对齐
- 逐页核对后确认无需修改：docs quick-start/server 各页/advanced/mrtr/what-is-mcp、wiki 版本常量与方法常量/错误码/守护测试名/序列化对计数/build.md 构建事实全部与代码一致

## 2026-08-14-00 增量审计复核（12 页修正 + 测试数值核算）

- 3 个子代理并行执行代码-文档配对审计（A：core/protocol/transport 链 12 页；B：server/client/http 链 14 页；C：工程页 build/tests/index 与新资产覆盖），主代理复核全部结论并消解冲突
- 修正 12 页 + index.md：
  - **modules/core.md**：非 const `operator[]` 行号 279→234；Serialize/Deserialize 成对函数「约 90 对」→ 实算 84 对（McpTypes.hpp 54 + Content.hpp 16 + Capabilities.hpp 9 + JsonRpc.cpp 5）
  - **modules/protocol.md**：删除「客户端不注册任何通知处理器」——客户端实注册 4 个（list_changed×3 + notifications/message，McpClient.cpp:450-454/541，另有公共 SetNotificationHandler :527）
  - **modules/transport.md**：`ITransport` 为 4 纯虚 + `IsStateless` 默认实现；PlatformIO 工厂补 `OpenStandardOutput/OpenStandardError`
  - **classes/wire-codec.md**：`NotInEra` 判定出自 `ValidateRequest`（非 `HasRequestMethod`）
  - **concepts/meta-and-filters.md**：过滤用途出处改为 ServerOptions/MessageFilter 头注释
  - **concepts/mrtr.md**：三件套字段修正（`InputRequestElicit`=message+可选 requested_schema；`InputRequests`=可选 confirm/elicit；`InputRequiredResult`=必填 input_requests+可选 request_state，McpTypes.hpp:269-282）；`auto_fulfill` 改为「显式配置 `input_required_config` 时默认开」
  - **classes/file-task-store.md**：写 `error_message` 的是 `CancelTask`（FileTaskStore.cpp:154-156），`SetTaskStatus` 仅改状态
  - **classes/http-server.md**：删除不存在的 `writer->isClosed()` 预检描述；SSE 写回调实捕获 `shared_ptr<TcpSocket>`（HttpServerImpl.cpp:389），impl 存活由 Stop 的 stopper 线程持有保证（HttpServer.hpp:94）
  - **concepts/oauth.md**：`VerifyResourceMatch` 行号 86→121
  - **classes/mcp-server.md**：`HandleInitialize` 补充「已配置 `options_.protocol_version` 时直接采用」前提（McpServer.cpp:1053-1060）
  - **modules/http.md**：`Mcp-Method` 区分客户端生成（StreamableHttpClientTransport.cpp:184）与服务端回显（StreamableHttpServerTransport.cpp:210，SEP-2243）
  - **build.md**：MSVC 标志补 `/wd4100` 与 `_CRT_SECURE_NO_WARNINGS`/`_SILENCE_ALL_CXX17_DEPRECATION_WARNINGS` 宏；job pool compile 上限 `cpu-2`（下限 1）
- 数值复核（静态统计 + ctest -N 实测 378 一致）：
  - 测试 15 目标 / 378 用例 / 881 断言（unit 13 目标 259 用例：core 116（含 JsonParserTests 75）、net 25、wire-codec 27、client 15、oauth 16、server 11、http 11、streamable-http 11、task-store 9、transport 6、message-filter 5、token-cache 5、websocket 2；integration 8；conformance 111）
  - tests.md 补齐 mcp-net-tests 行与 mcp-core-tests 的 JsonParserTests；合计 278→378、764→881
  - index.md「14 个 gtest 目标」→ 15
- 新资产覆盖核查：自研网络栈 9 文件、HttpServerImpl、JsonParser、sha256 均已有页面覆盖；唯一未覆盖为 NetStackTests/WebSocketClientTests（已随 tests.md 补齐）
- 全部 12 个修改页 timestamp 更新至 2026-08-14T00:57:41+08:00；同步 AGENTS.md（测试数量、OpenSSL 可选、版本协商头注释已同步）

## 2026-08-14-10 自研测试框架替换 googletest

- 新增 `tests/framework/`（mcp-test + mcp-test-main 静态库）：
  - 宏：`TEST`/`TEST_F` 套件注册、22 个断言宏（`EXPECT_/ASSERT_` 的比较/布尔/THROW/NO_THROW/STREQ/DOUBLE_EQ 族）
  - `ToString` 三路分派（数值/字符串/可打印对象）、`--gtest_filter` 兼容的过滤与 `--list-tests`
- `cmake/McpDiscoverTests.cmake`：`mcp_discover_tests()` 经 `TEST_INCLUDE_FILES` 逐用例注册 ctest（替代 `gtest_discover_tests`）
- `tests/framework/SelfTests.cpp`：框架自测 14 例（ToString、断言宏、子线程失败计数、TEST_F 生命周期、注册表）
- 数量：15 目标 / 392 用例（unit 12 目标 + integration 1 + conformance 1 + framework SelfTests 1），Debug+Release 双配置全绿
- 工作流兼容：`ctest -R 'XxxTest.CaseName'` 与直接跑二进制的 `--gtest_filter='XxxTest.*'` 均可用
- googletest 依赖移除：configure 无需联网（无 FetchContent 拉取）；文档同步清理（AGENTS.md、wiki/tests.md、wiki/build.md、wiki/index.md）
