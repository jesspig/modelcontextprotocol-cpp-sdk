# 2026-08-13 更新日志

## 2026-08-13-00 初始构建

- 建立 `wiki/` 目录结构：`modules/`、`classes/`、`transports/`、`concepts/`、`changelog/`
- 7 个子代理并行审计全部源码（30 个 src 文件 + 50 个公共头 + tests + CMake），素材全部带行号可溯源
- 撰写 28 个页面：
  - modules 6 页：core（JsonValue/JsonRpc/17 错误码/42 方法常量/5 版本）、transport（三态状态机/PlatformIO）、protocol（引擎/双时代 codec）、server、client、http
  - classes 9 页：json-value、mcp-session-handler、wire-codec、message-channel、mcp-server、mcp-client、http-server、file-task-store、file-token-cache
  - transports 5 页：stdio、sse、in-memory、websocket、streamable-http
  - concepts 6 页：version-negotiation、mrtr、meta-and-filters、concurrency、storage、oauth
  - build.md、tests.md（14 目标 / 271 用例 / 745 断言，静态统计）
- 数值核算：方法常量 42（25 请求 + 17 通知）、错误码 18、协议版本 5、通知类型 17、McpTypes 基础 13 / Params 23 / Results 19 / Options 5 / Notifications 4、任务页上限 1024、kMaxMessageSize 8MB、MessageChannel 64、HttpServer worker 8、stateless inflight 8
- 更新 AGENTS.md：新增 `## 项目知识库` 章节

## 2026-08-13-12 增量更新（代码大重构同步）

- 3 个子代理并行执行代码-文档配对审计（modules/classes、transports/classes、concepts/build/tests），逐项对照源码更新 26 页，主代理复核消解冲突
- 同步的重构变更（43 文件，+1495/−959）：Win32 管道 Overlapped I/O 化、传输 Close 无条件 join、WebSocket 循环引用修复、HttpServer Stop 线程化与 running_ 原子化、响应回发单 worker 队列、协议时代注册表严格对齐（2026 仅 10 请求方法 / 8 通知）、任务状态 wire 字符串化（tasks 2025-only）、缓存字段 2026 顶层平铺、Auto 回退对齐官方（-32022 三分支）、FileTaskStore 双锁与锁外写盘、OAuth 事后校验与刷新互斥、tools/list 序列化缓存、JsonFields 常量扩至 118
- 数值复核：测试 271→278（12 目标 / 764 断言）、通知类型 17→12（2026=8、2025=11）、协议版本 5→6（含 2024-10-07）、WireCodec 请求方法 23（公共 8 / 2025 独有 13 / 2026 独有 2）、方法集合改 unordered_set<string_view>
- 页面更新：modules 6 页全更、classes 9 页中 7 页更新（message-channel 无变化）、transports 4 页更新（in-memory 无变化）、concepts 4 页更新（mrtr 仅时间戳）、build.md 无变化、tests.md 全更
- 同步更新 AGENTS.md（Auto 回退/时代集合/缓存/任务状态/锁结构等 6 处过时描述修正）

## 2026-08-13-13 AGENTS.md 复核修正

- 核对 `git diff HEAD` 全部 75 个暂存文件后重写 AGENTS.md：
  - 数值修正：一致性测试 113→111（14 目标 / 278 用例）、协议版本 5→6（2024-10-07 起）、`Connect()` 明确为 `IClientTransport::Connect()` 返回 `shared_ptr`
  - OpenSSL 陷阱改述：无保护 `#include <openssl/rand.h>` 使 mcp-client 不可在无 OpenSSL 开发头的机器编译（原"可选"表述误导）
  - 补回 self-join 陷阱与 `PipeHandle::Read` 返回 0 判读要点（wiki/concepts/concurrency.md 有详述）
  - 新增 `## wiki 知识库维护` 章节：frontmatter/resource 规范、changelog 按天与时间戳格式、log.md 7 条上限、更新前 git 核查等全部规则
  - 补回提交规范（Conventional Commits + scope）

## 2026-08-13-17 自研 JSON 解析器替换 simdjson

- `refactor(core)`：移除 simdjson 依赖，新增自研递归下降解析器（[JsonParser.cpp](../../src/core/JsonParser.cpp)，`mcp::detail::json`）
- 特性：深度上限 512 层；数字超界分类——uint64 超 int64 抛 `DeserializeFailed`，其余语法/范围错误抛 `ParseError`，错误消息带 offset
- 新增 JsonParserTests 边界矩阵 75 例；新增 mcp-json-bench 基准，Debug/Release 对比达标
- 同步文档：README（中英）、docs installation/architecture（中英）、AGENTS.md、wiki/build.md、wiki/modules/core.md、wiki/classes/json-value.md 清除 simdjson 相关描述

## 2026-08-13-18 自研网络栈替换 libhv

- `refactor(transport|http)`：移除 libhv 依赖，新增自研网络栈 [src/transport/detail/net/](../../src/transport/detail/net/)（TcpSocket/TlsSocket/HttpClient/WebSocketClient/Sha1）与 [HttpServerImpl](../../src/http/HttpServerImpl.hpp)（HTTP/1.1 服务端，accept 线程 + 每连接线程，上限 256）
- 4 个使用方替换：SseClientTransport（双自研 HttpClient）、WebSocketClientTransport（自研 RFC 6455 客户端，无 onopen、`Start()` 直接 `SetConnected()`）、StreamableHttpClientTransport（POSIX 用自研 HttpClient，Win32 保持 WinHTTP）、HttpServer（自研 PIMPL 实现）
- 378 用例全绿（Debug+Release）；mcp-http-bench 基准达标（Debug 最差 69%，Release 更优，SSE 持平），记录见 [/tests/benchmark/baseline.md](../../tests/benchmark/baseline.md)
- libhv 依赖与 CMakeLists 改写补丁移除（FetchDependencies.cmake 不再拉取）
- 同步文档：README（中英）、docs installation/architecture/transports/oauth（中英）、AGENTS.md、wiki/build.md、wiki/modules/transport/http、wiki/classes/http-server、wiki/transports/sse/websocket/streamable-http、wiki/concepts/concurrency、wiki/tests.md、wiki/index.md 清除 libhv 描述
