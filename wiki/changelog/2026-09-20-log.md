# 2026-09-20 更新日志

## 2026-09-20-00 — URI 模板匹配接入、残留清理与测试计数刷新

- **新增 `src/detail/UriTemplate.hpp`**：RFC 6570 URI 模板匹配器（`Parse`/`Expand`/`Match`，上限 `kMaxUriTemplateLength`/`kMaxUriTemplateVariables`/`kMaxUriMatchLength`）——支持算子与变量列表解析、pct-encoded 解码、贪婪方向与长度截断；`Parse` 以 `(tmpl, error)` 返回 `std::optional`，非法模板不抛异常（由调用方决定错误码）
- **路由接入**（[McpServer.cpp](../../src/server/McpServer.cpp)）：`HandleReadResource` 改两轮遍历——第一轮**静态资源优先**（跳过 `is_template`，`uri_pattern == uri` 调 `handler(uri)`），未命中再逐模板 `detail::UriTemplate::Parse` + `Match`，命中调 `template_handler(uri, variables)`（变量名 → pct-decoded 值，[McpServer.hpp:171](../../include/mcp/server/McpServer.hpp)）；两轮均未命中仍抛 `InvalidParams "resource not found: <uri>"`，命中分支照旧回填 `resources/read` 的 cache hint
- **行为变更**：`RegisterResourceTemplate` 先用 `detail::UriTemplate::Parse` 校验模板，**非法模板抛 `McpError(InvalidParams, "invalid resource template '<tmpl>': <reason>")`**（[McpServer.cpp:329](../../src/server/McpServer.cpp)）——此前不校验即写入注册表
- **测试**：新增目标 `mcp-uri-template-tests`（[UriTemplateTests.cpp](../../tests/unit/UriTemplateTests.cpp)，20 用例 / 129 断言）；`mcp-server-tests` 41 → 46 用例，含模板路由与校验守护用例 `ReadResourceMatchesRegisteredTemplate`、`ReadResourceTemplateValueIsPctDecoded`、`ReadResourcePrefersStaticOverTemplate`、`RegisterResourceTemplateRejectsInvalidTemplate`（[McpServerTests.cpp:113](../../tests/unit/McpServerTests.cpp)）
- **conformance**：官方 conformance server leg（主代理实测）exit 0、`Baseline check passed`，`resources-templates-read: 2 passed, 0 failed`——[tests/conformance/baseline.yaml](../../tests/conformance/baseline.yaml) 的 `resources-templates-read` 条目按「基线只减不增」删除，server 侧现存 2 条（`tools-call-with-progress`、`server-sse-multiple-streams`）
- **残留清理**：删除 16 个零调用 `Serialize*/Deserialize*` 公共函数及其连带的 `DeserializeRoot`（共 17 个）；删除已成空壳的 `src/core/McpTypesOptions.cpp` 并从根 [CMakeLists.txt](../../CMakeLists.txt) 的 `mcp-core` 源列表移除；`TrimWhitespace` 与 `NetIoUtil.hpp` 的 `TrimInPlace` 合并为单一来源（现存 [NetIoUtil.hpp](../../src/transport/detail/net/NetIoUtil.hpp)，HttpClient/WebSocketClient/StreamableHttpServerTransport 共用）
- **三处重复实现经复核维持现状（不合并）**，理由如下，避免后人重复评估：
  - base64url：[RequestState.hpp](../../include/mcp/server/RequestState.hpp) 为 server 层 **header-only inline** 实现（RFC 4648 §5 无填充，供 requestState 签发/校验）；[OAuthClientProvider.cpp](../../src/client/auth/OAuthClientProvider.cpp) 的实现属 client 库编译单元；[Sha1.hpp:115](../../src/transport/detail/net/Sha1.hpp) 的 `Base64Encode` 另为标准字母表 + `=` 填充（WebSocket 握手）。字母表/填充语义与所在库均不同，合并需新增公共头与语义开关
  - SSE 解析器：[StreamableHttpClientTransport.cpp:102](../../src/http/StreamableHttpClientTransport.cpp) 的 `ParseSseBlock` 只取 `data:`/`id:`，产出 `SseBlockParseResult{data, event_id, message}`；[SseClientTransport.cpp:63](../../src/transport/SseClientTransport.cpp) 的 `ParseSseEvent` 另解析 `event:`/`retry:`，产出 `SseEvent{event_type, data, id, retry_ms}`。两者分属 mcp-http / mcp-transport，输出结构不同（后者面向 legacy SSE 客户端）
  - 大小写归一：[NetIoUtil.hpp:16](../../src/transport/detail/net/NetIoUtil.hpp) 的 `ToLower` 返回**新串**，而 mcp-http 各处的就地归一/不敏感比较（[HttpServerImpl.cpp:695](../../src/http/HttpServerImpl.cpp)、[StreamableHttpServerTransport.cpp:436](../../src/http/StreamableHttpServerTransport.cpp)、[StreamableHttpClientTransport.cpp:902](../../src/http/StreamableHttpClientTransport.cpp)）用逐字符 `std::tolower` 原地改写或直接比较，**不分配新串**——合并会把无分配路径改成分配路径
- **知识库数值刷新（口径全部实测）**：[tests.md](../tests.md) 目标清单重列 **18 目标 / 612 用例 / 2048 断言**（新增 `mcp-uri-template-tests` 行；`mcp-server-tests` 46/253、`mcp-client-tests` 52/232、`mcp-http-tests` 28/119、`mcp-conformance-tests` 122/378）；正文口径说明改为用例数「ctest 注册口径」（与静态 `^(TEST|TEST_F)\(` 计数 565 + 47 = 612 一致）与断言数「`tests/**/*.cpp` 静态统计，不含框架头文件」；[AGENTS.md](../../AGENTS.md)「17 目标 / 578 用例」→「18 目标 / 612 用例」；[index.md](../index.md) 目标数同步；[modules/core.md](../modules/core.md) 序列化函数改为逐声明头实测（McpTypes.hpp 49 `Serialize*` + 45 `Deserialize*`/44 对、Content.hpp 16 对、Capabilities.hpp 9 对、6 个单边函数）并补 `UriTemplate.hpp` 内部头条目；[modules/server.md](../modules/server.md) 与 [classes/mcp-server.md](../classes/mcp-server.md) 记入模板路由与 `InvalidParams` 行为变更
- **验证**：全量 `ctest` 由主代理执行，记录 612/612 全部通过（`build/debug/Testing/Temporary/LastTest.log` 逐用例 `Test Passed`，0 失败）；官方 conformance server leg exit 0 且 `Baseline check passed`；client leg（`--suite core`）exit 0 且 `Baseline check passed`

## 2026-09-20-01 — 协议合规补全、重复实现收敛与授权增强

- **协议版本拒绝**（[McpSessionHandler.cpp:308](../../src/protocol/McpSessionHandler.cpp)）：服务端对非 `initialize` 请求检查 `_meta.protocolVersion`，不在 `kProtocolVersions` 内即回 `UnsupportedProtocolVersion`（-32022），`data` 带 `requested` 与服务端 `supported` 列表；此前服务端接受任意版本声明，客户端 Auto 模式的 corrective 重试因此无从触发
- **`_meta` 字段类型校验**（[McpSessionHandler.cpp:58](../../src/protocol/McpSessionHandler.cpp)）：派发前按 `InvalidMetaField` 检查 `protocolVersion`（字符串）、`progressToken`（字符串或整数）、`clientInfo`/`clientCapabilities`（对象）、`logLevel`/`traceparent`/`tracestate`/`baggage`（字符串）；不符回 `InvalidParams`（消息 `invalid _meta field: <键名>`），未识别键放行
- **协议级取消接线**（[McpSessionHandler.cpp:801](../../src/protocol/McpSessionHandler.cpp)、[McpServer.cpp:1038](../../src/server/McpServer.cpp)）：在途请求建立取消标志（`GetIncomingCancellationFlag` / `EraseIncomingCancellationFlag`），`notifications/cancelled` 置位并清理 pending，服务端派发前把标志注入 `RequestContext`——**普通（非任务）工具 handler 的 `IsCancellationRequested()` 此前恒为 false，现在能真实反映**
- **订阅接线**（[McpServer.cpp:389](../../src/server/McpServer.cpp)）：新增 public `SendResourceUpdated(uri)`；`SendToolListChanged` / `SendResourceListChanged` / `SendPromptListChanged` 经 `SendListChangedNotification` 按时代分派（2026 走 `NotifySubscribers` 按 filter 过滤、2025 及更早广播）；修正 2025-era `resources/subscribe` 登记时 filter 为空的问题
- **OAuth 授权增强**（[OAuthClientProvider.cpp](../../src/client/auth/OAuthClientProvider.cpp)）：`OAuthClientOptions::resource` 新增（RFC 8707，取值优先级「显式 → 已发现元数据 → `server_url`」，非空时注入授权 URL 与三个令牌请求）；`AuthorizationCodeResult::iss` 新增，授权响应侧在 state 校验之后、换取令牌之前按 RFC 9207 比对 issuer；CIMD 落地——`client_id` 为 URL 时拉取并解析 Client ID Metadata Document（best-effort，失败回退已配置值并记 Warning）
- **重复实现收敛（推翻本日 00 条的「维持独立」结论）**：
  - 大小写归一：`src/http` 与 `src/transport` 的 4 处手写小写化统一到新增 [StringUtils.hpp](../../include/mcp/detail/StringUtils.hpp)
  - base64url：[RequestState.hpp](../../include/mcp/server/RequestState.hpp) 与 [OAuthClientProvider.cpp](../../src/client/auth/OAuthClientProvider.cpp) 两份实现收敛到新增 [Base64Url.hpp](../../include/mcp/detail/Base64Url.hpp)（含 `Base64UrlDecode`）
  - SSE 解析：[StreamableHttpClientTransport.cpp](../../src/http/StreamableHttpClientTransport.cpp) 与 [SseClientTransport.cpp](../../src/transport/SseClientTransport.cpp) 的行解析层抽取到新增 [SseEventParser.hpp](../../include/mcp/detail/SseEventParser.hpp)，两侧各自语义不变
  - 00 条曾以「输出结构不同/无分配路径」为由维持独立，现改为共享底层实现
- **脚本修复**（[run-conformance.sh](../../scripts/run-conformance.sh)）：`BIN_DIR` 改按 `<preset>/examples/conformance/<target>/<target>` 解析（原缺 target 子目录，server 模式无法运行），并保留旧布局回退
- **文档**：docs 中英同步 5 页（client/oauth、advanced/protocol-versions、server/overview、server/resources）；wiki 更新 oauth、version-negotiation、meta-and-filters、mcp-session-handler、mcp-server、server、core 共 7 页

## 2026-09-20-03 — 知识库增量审计与最新源码同步

- **审计基线**：核查 `git status`/`git diff HEAD` 后确认工作区初始无未提交变更；按当前源码重新执行 `cmake --preset debug` 与 `ctest --preset debug -N`，得到 18 个测试目标、632 个 ctest 用例；`TEST`/`TEST_F` 静态计数为 585 + 47 = 632，断言 token 统计为 2154
- **新增源码行为**：同步 SEP-2243 `x-mcp-header` 注解解析、客户端 `Mcp-Param-*` 注解镜像、服务端 body/header 一致性校验与 HeaderMismatch；同步 `SpanHooks` 的三类边界事件；同步 `HttpClient` 8KB 读缓冲与 `mcp-transport-bench` 基准目标
- **现有页面修订**：更新 core/protocol/client/server/http/transport/build/tests、Streamable HTTP、McpClient/McpServer/McpSessionHandler、并发/存储/OAuth/日志、FileTokenCache 和 MRTR 页面；方法/通知常量修正为 41；移除客户端“不支持 x-mcp-header”的过时描述；补充当前测试明细和失败语义
- **新增页面**：`concepts/uri-template.md`、`concepts/response-cache.md`、`concepts/mcp-param-headers.md`、`concepts/span-hooks.md`，均含 frontmatter、源码 `resource`、出入站互链并加入 `index.md`
- **规则与导航**：`AGENTS.md` 测试基线更新为 632，并补齐 `## 项目知识库`；索引测试入口和最新 changelog 链接更新；同步 `HttpServerTests.cpp` 中关于注解支持的过时测试注释
- **验证状态**：`cmake --build --preset debug` 成功；`ctest --preset debug --output-on-failure` **632/632 全部通过**，总耗时约 257 秒；frontmatter/链接/数值静态核查与 ctest 注册枚举也已完成

## 2026-09-20-17 — Windows GetObject 宏修复与 NOGDI

- **变更基线**：`git status`/`git diff HEAD` 确认 6 文件 47 增 2 删，未碰源码之外的测试/基线（`tests/conformance/baseline.yaml` 与 conformance fixture 未动）
- **编译选项**（[CompilerOptions.cmake](../../cmake/CompilerOptions.cmake)）：clang-cl 与 MSVC 两分支 `NOMINMAX` 后追加 `NOGDI`——`NOGDI` 排除 `wingdi.h` 的 GDI 定义（含与 `JsonValue::GetObject` 冲突的 `GetObject` 宏），`NOMINMAX` 排除 `min`/`max` 宏
- **头侧隔离**（[JsonValue.hpp](../../include/mcp/JsonValue.hpp) 标记 `MCP_POP_GETOBJECT_MACRO`、[McpParamAnnotations.hpp](../../include/mcp/detail/McpParamAnnotations.hpp) 标记 `MCP_POP_GETOBJECT_MACRO_ANNOT`）：顶部 `#ifdef GetObject` 内 `push_macro` + `undef`，尾部按标记条件 `pop_macro` 恢复
- **源侧收敛**（[HttpServerImpl.cpp](../../src/http/HttpServerImpl.cpp) 标记 `MCP_CPP_POP_GETOBJECT_HTTPSERVER`、[StreamableHttpClientTransport.cpp](../../src/http/StreamableHttpClientTransport.cpp) 标记 `MCP_CPP_POP_GETOBJECT_CLIENT`、[StreamableHttpServerTransport.cpp](../../src/http/StreamableHttpServerTransport.cpp) 标记 `MCP_CPP_POP_GETOBJECT_SERVER`）：`push_macro` 移入 `#ifdef GetObject` 内并配尾部条件 `pop_macro`——宏未定义（含 `NOGDI` 生效）时不再空 `push`，且恢复包含前宏状态；此前 `HttpServerImpl`/`Client` 无尾部恢复，`Server` 的 `push` 在 `#ifdef` 之外
- **知识库同步**：更新 [build.md](../build.md)（`NOMINMAX` → `NOMINMAX` + `NOGDI`，清除“仅 NOMINMAX”过时描述）、[modules/transport.md](../modules/transport.md)（“未定义 NOMINMAX 时”过时表述改为全局定义 + 括号纵深）、[classes/json-value.md](../classes/json-value.md)（头侧隔离）、[modules/http.md](../modules/http.md)（源侧三文件条件隔离）、[concepts/mcp-param-headers.md](../concepts/mcp-param-headers.md)（注解头隔离）；5 页 `timestamp` 同步为本次真实时间
- **验证状态**：未执行编译/测试（任务约束）；以 `git diff HEAD` 为事实标准逐项核对，无无法核实项

## 2026-09-20-18 — Streamable HTTP 服务端请求有界等待监听者

- **变更基线**：`git status`/`git diff HEAD` 确认 5 文件 35 增 1 改（`include/mcp/http/HttpServer.hpp`、`src/http/HttpServer.cpp`、`src/http/HttpServerImpl.hpp`、`src/http/HttpServerImpl.cpp`、`src/http/StreamableHttpServerTransport.cpp`），未碰 `tests/conformance/baseline.yaml` 与 fixture
- **新增 `HttpServer::SseClientCount`**（[HttpServer.hpp](../../include/mcp/http/HttpServer.hpp) 声明、`HttpServer.cpp` 经 `atomic_load(impl_)` 转发空 impl 返回 0、`HttpServerImpl.hpp/cpp` 持锁返回 `sse_clients_.size()`，`sse_mutex_` 为支持 const 方法改为 `mutable`）
- **有界等待**（[StreamableHttpServerTransport.cpp](../../src/http/StreamableHttpServerTransport.cpp)，`SendMessageAsync` 内 pending 匹配之后、广播之前）：仅 `IsRequest`（`JsonRpcRequest`）且 `SseClientCount() == 0` 时以 50ms 步长（`kSseListenerWaitStep`）轮询至多 2s（`kSseListenerWaitBudget`，新增 `<thread>` 依赖）；`TransportState::Disconnected` 可中断退出；超时记 Warning（`server-initiated request '<method>' broadcast with no SSE listener`）后仍走广播路径；**Notification 不等待、保持 fire-and-forget**
- **背景结论**：CI 中 `tools-call-sampling` 约 30s 超时、`elicitation-sep1330-enums` 报 1 过 1 挂，本地复现均通过（sampling 2/2、sep1330 6/6）；根因为 server-initiated 请求只走 GET 广播流、快照为空时静默丢弃，referee GET 建连抖动即丢失；修复后全量 66 passed 1 failed（仅基线内 progress），baseline 门 exit 0；未增补基线条目（flaky 通过项入基线会触发过期条目反杀）
- **知识库同步**：更新 [transports/streamable-http.md](../transports/streamable-http.md)（`SendMessageAsync` 有界等待语义 + GET 段指向，清除"快照为空即广播静默丢弃适用于请求"的过时描述）、[modules/http.md](../modules/http.md)（`SseClientCount` 要点）、[classes/http-server.md](../classes/http-server.md)（`SseClientCount` 要点）；3 页 `timestamp` 同步为本次真实时间（`2026-09-20T18:14:09+08:00`）；`modules/transport.md` 无需改动（`SseClientCount` 归属 mcp-http，已有链路指向 streamable-http 页）
- **验证状态**：主代理已执行 `cmake --preset debug` 配置、`cmake --build --preset debug` 全量 70/70 通过、`mcp-http-tests` 35/35 与 `mcp-streamable-http-tests` 40/40 通过、官方 conformance 全量 66 passed 1 failed（仅基线内 `tools-call-with-progress`）且 baseline 门 exit 0
