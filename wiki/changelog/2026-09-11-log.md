# 2026-09-11 更新日志

## 2026-09-11-04 — _meta 线上位置修正：JSON 顶层 → params._meta

- 请求/通知的 `_meta` 线上位置从 JSON 顶层改为 `params._meta`（对齐官方规范与 TS 服务器）；序列化层（`src/core/JsonRpc.cpp`）新增 `MergeMetaIntoParams`/`ExtractMetaFromParams`——序列化把 `meta` 合并进 `params._meta`，反序列化取出后从 params 移除 `_meta` 键；内存结构 `req.meta`/`notif.meta` 语义不变
- `WireCodec` Rev2026：`ValidateRequest` 改查 `params._meta`（除 `server/discover` 外缺失即 Invalid）；`StampOutgoingRequest` 写 `params._meta`
- `McpSessionHandler`：入站轻量校验视图把 `req.meta` 置于视图 params 内（与线上一致）；`SendRequest` 双 era 分支——modern era `req.meta` 写 `_meta` 信封，legacy era progressToken 写 `params._meta.progressToken`
- `include/mcp/JsonRpc.hpp` 两行过时注释同步修正（原描述"top-level _meta"）

## 2026-09-11-04 — progress 双向通道：服务端 SendProgress + 客户端 on_progress 回调

- `McpServer::SendProgress(token, progress, total?, message?)`（`include/mcp/server/McpServer.hpp` + `src/server/McpServer.cpp`）：向客户端发 `notifications/progress`，token 原样透传，`total`/`message` 可选
- `RequestOptions` 新增 `on_progress` 回调（`include/mcp/McpTypes.hpp`）；`McpClient` 经 `AttachProgressCallback` 注册回调并生成 progressToken（`options.meta.progressToken` 显式提供时优先，否则原子计数自 1 起自动生成），请求结束 RAII（`ScopedProgressCleanup`）清理
- `WireClientHandlers` 新增 `notifications/progress` 处理器：`ResetTimeoutByProgressToken` 顺延请求 deadline 后按 token 分发回调；回调在会话消息循环线程同步执行、须快速返回
- 缺陷修复：`RequestContext` 由存 `JsonRpcRequest` 指针改为**存值**——异步工具 handler 延迟执行时原请求对象可能已析构，存值使 `GetRequest()` 在异步场景安全

## 2026-09-11-04 — 客户端 SendRootsListChanged（版本门控）

- `McpClient::SendRootsListChanged()`：发送 `notifications/roots/list_changed`，协商版本 **>= 2025-06-18**（`kProtocolVersions` 序位比较）才允许，否则抛 `McpError(ProtocolViolation)`

## 2026-09-11-04 — Streamable HTTP 客户端 GET SSE 接收流

- `HttpClientTransportOptions` 新增 `enable_listen_stream`（**默认 true**）：发送 `notifications/initialized` 后自动在独立 `mcp-listen` 线程开 GET SSE 长流（WinHTTP/POSIX 两平台一致），服务端推送的通知经 SSE 分块解析后经 MessageChannel 并入会话引擎分发
- 405 视为服务器不支持（`ListenState::Unsupported`，静默放弃）；断线退避重连 1s 起倍增封顶 30s、最多 5 次（超限 `GivenUp`），重连携带 `Last-Event-ID`；单流读超时 600s
- `Close()` 顺序：先停 listen 流（句柄中断在途 GET 并 join 线程）再关 POST 通路，避免 Close 阻塞在 GET 读上

## 2026-09-11-04 — MCP-Protocol-Version 头自学习

- Streamable HTTP 客户端 initialize 请求**不再携带** `MCP-Protocol-Version` 头；从 initialize 响应 `result.protocolVersion` 学习（POST 响应体与 GET 流首帧均可学习），后续请求与 GET 流按学习值携带，无学习值兜底 `2026-07-28`（`ProtocolVersionHeaderFor`/`EffectiveProtocolVersion`/`NegotiatedVersionFromResponse`）

## 2026-09-11-04 — 测试扩充与官方互操作联调

- 新增集成测试 `tests/integration/Phase1InteropTests.cpp`（4 用例：InMemory/HTTP 的 progress 双向、`SendRootsListChanged`、GET SSE 接收 `tools/list_changed`）；SessionHandler/WireCodec/StreamableHttp/McpClient/McpServer 各单元测试新增用例；全量 16 目标 / **482 用例**（`ctest -N` 实测）
- 与 TS conformance 服务器（2025-11-25 legacy）人工联调 **7/7 通过**：协商、tools/list、progress 0/50/100、roots 通知、logging 通知

## 2026-09-11-04 — wiki 知识库同步

- 更新 modules（client/server/http/protocol）、classes（mcp-client/mcp-server/mcp-session-handler/wire-codec）、transports/streamable-http、concepts（version-negotiation/meta-and-filters）、tests（用例统计 445→478 静态 / 482 运行期）；清除"顶层 stamp `_meta`""固定带 2026-07-28 版本头"等过时描述，同步漂移行号引用

## 2026-09-11-08 — 服务端 RequireInitialized era 门控（U2）

- `McpServer` 初始化守卫统一经 `RequireInitialized(initialized, modern_era, promise)`：modern era（2026-07-28）直接放行（Pin-to-2026/纯 modern 客户端无 initialize 握手可用），legacy era 未初始化仍回 `InvalidRequest "Server not initialized"`

## 2026-09-11-08 — tasks 服务端自动化（U3/U4）

- `ToolOptions::execution`（`ToolExecutionMode::Task`）声明任务化工具；`tools/call` 任务化路径立即返回 `CreateTaskResult`（wire `task.taskId/status/createdAt`，`resultType` 落 `"task"`），仅 2025 及更早时代可用
- 后台 `std::async` 执行，状态流转 Working→Completed/Failed 经 `notifications/tasks/status` 通知；失败任务把首个文本 content 写入 store 作 `error`
- 协作取消：`RequestContext` 构造新增 `cancellation_flag`（`shared_ptr<const std::atomic<bool>>`）与 `IsCancellationRequested()`；`tasks/cancel` 置位标志 + 落 `Cancelled` 终态，终态不迁移
- `tasks/get/update/cancel/result/list` era gate 不变（仅 2025 及更早）；FileTaskStore 持久化任务结果

## 2026-09-11-08 — tasks 客户端流与总量超时（U5/U9）

- `McpClient::CallToolAsTask`：任务化 tools/call 立即返回任务句柄，与 `GetTask`/`PollTaskToCompletion`/`CancelTask` 串成完整流；`ClientOptions::max_total_timeout` 构造期接线至会话引擎
- `McpSessionHandler::SetMaxTotalTimeout`：每请求 `SendRequest` 时记录绝对截止（`absolute_deadlines_`），progress 续命只顺延 idle deadline 不可越过总量（默认 0 禁用）

## 2026-09-11-08 — URL elicitation（SEP-1034，U6/U7）

- `ElicitRequestParams` 扩展 `mode`/`url`/`elicitationId`（form 模式 wire 不写新字段，向后兼容）；`ElicitResult` wire 键修正 `values`→`content` + 新增 `action`（对齐规范与官方服务器）
- 服务端 `McpServer::ElicitUrl(url, message, timeout=600s)`：发 url 模式 `elicitation/create` → 挂起等 `notifications/elicitation/complete` 唤醒 → `action="accept"`；watchdog 超时抛 `RequestTimeout`
- 客户端 `SetUrlElicitationHandler`：url 缺 `elicitationId` 抛 `InvalidParams`，handler 返回后自动发送 complete 通知

## 2026-09-11-08 — 404 会话自愈（U8）

- 新增 `SessionExpired = -32009` 错误码；传输层 404 且 body 非 JSON-RPC error 时类型化交付（`MakeSessionExpiredError`）
- `ClientOptions::reinit_on_expired_session`（默认 true）：`McpClient` 捕获后 `RecoverExpiredSession()` 重协商 + 原请求恰一次重放（`session_generation_` 世代计数 + `reinit_mutex_` 防并发重复）；initialize 请求不带 `Mcp-Session-Id` 头

## 2026-09-11-08 — 翻页聚合与不收敛防护（U10）

- 新增 `ListToolsAll/ListResourcesAll/ListResourceTemplatesAll/ListPromptsAll`（64 页上限防不收敛，超限抛 `ProtocolViolation`）
- 修复 `ListPages` 无 cursor 自动聚合不收敛时静默截断的缺陷——改为抛 `McpError(ProtocolViolation)`；常量更名 `kMaxListPages`

## 2026-09-11-08 — 传输层联调缺陷修复

- 发送侧死锁：Notification/Response 改为独立即时 POST（`LaunchImmediatePost`，detached 线程保活），Request 保持串行队列——解开 server→client 请求场景互等
- 接收侧：Win32/POSIX `DoPost` 改为边读边分块分发（POST 响应 SSE 流打开期间 server→client 请求即可达）
- Win32 `WinHttpReadData` 填满缓冲语义改为 `WinHttpQueryDataAvailable` 增量读（DoPost SSE 与 DoListenGet 同修）
- discover 协商两缺陷：`serverInfo` 容缺（顶层缺失时从 `_meta["io.modelcontextprotocol/serverInfo"]` 提取）；响应 `supportedVersions` 与客户端支持表求交集（`DeclaresSharedClientVersion`/`SelectSharedVersion`，空/无交集回退，交集取最新）

## 2026-09-11-08 — 官方联调与测试扩充

- TS conformance 服务器（3010）双向验证：Auto 模式 modern era（2026-07-28）首次走通（discover→tools/list→tools/call）；Legacy 模式（2025-11-25）form elicitation 官方互通全链路（`content` 数据被官方服务器消费）；modern era 下 TS 服务器拒绝 server→client 请求属官方正确行为
- 新增 `tests/integration/Phase2ClosureTests.cpp`（7 用例）；SessionHandler/McpServer/McpClient/StreamableHttp 各单测扩充；全量 **16 目标 / 521 用例**（`ctest -N` 实测）
