# 2026-09-15 更新日志

## 2026-09-15-00 — CI 与 conformance 修复：URL elicitation 竞态、SSE keepalive、就绪探测

- **服务端 URL elicitation 注册先于发送**（[McpServer.cpp:472-489](../../src/server/McpServer.cpp)）：`ElicitUrl` 把 `pending_url_elicitations_` 注册移到 `SendRequest` 之前——complete 通知可能先于注册到达被静默丢弃、promise 永不完成；`SendRequest` 抛异常时清理已注册条目后 rethrow
- **超时消息区分与未知 id 日志**（[McpServer.cpp:497-523](../../src/server/McpServer.cpp)）：`url elicitation timed out (no response)`（请求无应答）与 `(no complete notification)`（响应已到但 complete 未到）分别报错、保留前缀；complete 通知携带未知 `elicitationId` 时丢弃并 `MCP_LOG(Warning, ...)`（原为静默 return）
- **客户端 complete 先于响应**（[McpClient.cpp:545-557](../../src/client/McpClient.cpp)）：URL elicitation 处理先发 `notifications/elicitation/complete`、再 `p.set_value` 提交响应；通知发送异常被捕获仅记 Error，不影响响应提交
- **SSE keepalive 忙循环修复恢复（f5c7262）**（[HttpServerImpl.cpp:840](../../src/http/HttpServerImpl.cpp)）：`KeepAliveLoop` 的 `wait_for` 谓词恢复为仅 `!running_.load()`——旧谓词 `!running_ || !sse_clients_.empty()` 使有 SSE 客户端时恒真、`wait_for` 立即返回，keepalive 线程忙循环烧 CPU；该修复曾以 f5c7262（`fix(网络栈): 修复 SSE keepalive 谓词反转忙循环致 macOS CI 超时`）提交，但该提交不在当前分支历史（无分支包含，dangling），本轮恢复。现每轮睡满 `sse_keep_alive_ms` 后广播 ping，仅 `Stop()`（`running_` 置 false + `notify_all`）唤醒退出
- **conformance CI 配置**（[.github/workflows/conformance.yml](../../.github/workflows/conformance.yml)、[run-conformance.sh](../../scripts/run-conformance.sh)）：server/client 双 job `node-version` 20 → 22（referee 依赖 Node 22+ 的 `fs.globSync`）；server 就绪探测由 `curl -s --max-time 2 GET /mcp` 改为 `probe_ready()` POST `initialize`——GET 会建立 SSE 长流使 `--max-time 2` 必然超时（退出码 28）
- **验证**：`Phase2InMemoryElicitFixture.UrlElicitationRoundTrip` `--gtest_repeat=100` 零失败；相关 19 用例通过（含 `SseKeepAliveFrames`）；全量 ctest 578/578 通过；本地 conformance-server 实测 POST `initialize` 0.15s 返回 HTTP 200（旧 GET 探测 curl 退出码 28）

## 2026-09-15-06 — conformance client sse-retry：退避睡眠语义恢复与 SSE 空 data 解析修复

- **客户端退避睡眠语义颠倒恢复（丢失提交 8363d75 等价恢复）**（[StreamableHttpClientTransport.cpp:142-149](../../src/http/StreamableHttpClientTransport.cpp)）：`SleepInterruptibly` 谓词由 `!stop.load()` 改为 `running.load()`（参数 `stop` → `running`，调用点传 `running_`）——原实现运行中 `running_=true` 时 `!stop` 恒假、退避睡眠从不生效，Close 后置 false 反而睡满；修复后运行中睡满退避时长、`Close()` 可打断。与未进入任何分支历史的 dangling 提交 8363d75（`fix(网络栈): 修复客户端退避睡眠语义颠倒并补投 SSE 首连前丢失的广播`）中该文件的改动逐字一致（含注释）
- **SSE 空 data 解析缺陷**（[StreamableHttpClientTransport.cpp:108-113](../../src/http/StreamableHttpClientTransport.cpp)）：`data:` 冒号后仅空白时，原实现 `find_first_not_of` 返回 `npos` 后保留原值（如 `" "`），`result.data` 非空 → `DeserializeMessage` 失败 → `DispatchSseBlock` 报 Error `HTTP SSE block parse failed`；referee sse-retry 的 priming 事件 `id`/`retry`/`data:`（空 data）命中该路径。现改为 `npos` 时产出空串，`DispatchSseBlock` 开头 `if (parsed.data.empty()) return;` 静默忽略
- **client 基线条目恢复 `sse-retry`**（[baseline.yaml:50-55](../../tests/conformance/baseline.yaml)，client 段 36 → 37 条）：归因——server 关流后 client 未等待 retry 字段声明的 500ms 即重连（实测 -70ms，要求 >=450ms 且 <=1000ms）；另有 WARNING/SHOULD：重连未携带 Last-Event-ID 头。条目注明"实现 retry 等待与 Last-Event-ID 后删除本条目"（届时 runner 以过期条目报错）；该条目曾存在于历史 baseline（对照 `git show f5c7262:tests/conformance/baseline.yaml`，当时 retry 计时实测为 -112ms）
- **验证**（本地 Node 24 + referee 0.2.0-alpha.11）：client leg `Baseline check passed` exit 0（sse-retry 命中基线条目）；server leg exit 0（65 passed / 2 failed 均在基线）；全量 ctest 578/578 通过

## 2026-09-15-15 — 发布准备：版本号更新至 0.3.4

- **版本号**（[McpVersion.hpp:9](../../include/mcp/McpVersion.hpp)）：`kSdkVersion` `"0.3.3"` → `"0.3.4"`——版本号唯一来源，`CMakeLists.txt` 自动提取为 `PROJECT_VERSION`，本次无需改 CMake；`ClientOptions::client_info` 默认标识随之更新
- **在线文档**：`docs/zh/client/overview.md:22`、`docs/en/client/overview.md:22` 的默认客户端标识 `{"mcp-cpp-client","0.3.3"}` → `0.3.4`
- **知识库**：[wiki/classes/mcp-client.md:43](../classes/mcp-client.md) 的 `ClientOptions` 默认描述 `client_info` 版本 `0.3.1` → `0.3.4`（该处此前滞后于代码）
- **扫描结论**：docs 全量（zh/en，排除 node_modules 与 .vitepress 缓存）中 SDK 版本引用仅上述 2 处——无 `0.3.x`、"latest version"、版本表/兼容性表需改；SSE keepalive 相关仅 `guide/transports.md` 配置表一行（"注释帧间隔，0 禁用"），与 `KeepAliveLoop` 实现（睡满 `sse_keep_alive_ms` 广播 ping、仅 `Stop()` 唤醒退出）一致，未改

## 2026-09-15-15 — 知识库全量一致性审计（22 页源码对照修正 + AGENTS.md 核对）

- **数值复核**：`ctest -N` 实测 578 用例与静态 `^TEST` 宏计数一致；17 个测试目标逐目标用例/断言加总与 [tests.md](../tests.md) 表格完全吻合（578 / 1836，断言口径不含框架头文件）；frontmatter 全检（30 内容页 type 同类一致、timestamp/resource 齐备）、链接全检 205 条
- **死链修复**（[modules/http.md:41](../modules/http.md)）：`](classes/http-server.md)` → `](/classes/http-server.md)`（唯一死链，其余 204 条有效）
- **core/protocol 修正**：错误码 19→**20**（补 `SessionExpired(-32009)`，且 `default_error_condition` 映射补记）；序列化配对数 87→**88** 对（McpTypes.hpp 57 + Content 16 + Capabilities 9 + JsonRpc 6，`SerializeTaskStatusNotificationParams` 单边）；Results 名单 `InputRequiredResult`（实为不继承）→ `CreateTaskResult`；Notifications 4→**5** 类（补 TaskStatus 参数）；JsonFields 118→**119**（补 6 个 `_meta` 信封键与 2 个 ResultType 值字符串）；double 序列化表述更正（`to_chars` 最短往返，`max_digits10` 仅 `snprintf` 回退路径）；`Get*` 访问器 7 组→**6 组**（GetArray/GetObject 含重载）——[core.md](../modules/core.md)、[json-value.md](../classes/json-value.md)
- **门面修正**：`WireCoreHandlers` 补 `notifications/elicitation/complete` 处理器（URL elicitation 唤醒路径）；McpServer/McpClient/FileTokenCache 页行号全面校正；Auto 模式 `-32022` 三分支按代码判据重写（含 `kLatestProtocolVersion` 与 supported 缺失/格式不符回退 initialize）；`PollTaskToCompletion` 失效/取消仅返回终态、超时才抛 `InternalError`；`SendRequestWithMrtr` 包装 `SendRequestWithMrtrOnce`；FileTokenCache 仅三个公开方法持锁（Load/Save 由构造/析构直调）——[server.md](../modules/server.md)、[mcp-server.md](../classes/mcp-server.md)、[client.md](../modules/client.md)、[mcp-client.md](../classes/mcp-client.md)、[file-token-cache.md](../classes/file-token-cache.md)
- **传输修正**：POSIX PATH 搜索限定自定义环境分支（默认分支由 `execvp` 子进程内搜索）；SIGPIPE 表述补 `SO_NOSIGPIPE` 空操作平台走 `MSG_NOSIGNAL`；SSE `retry:` 接受全数字毫秒值（含 0）；SSE 缓冲超限防护实为 `body_cb` + `http_client_->Close()`（原误记 `http_cb`/`send`）；WebSocket `Close()` 置空回调后**不再回调会话**（含 on_close），置空理由更正（回调仅捕获 weak_ptr）；并发页 Streamable HTTP 客户端线程模型重写（发送线程 + `mcp-listen` 监听线程 + 短命 detached POST）、`Close()` 顺序更正、self-join detach 例证由不存在的 `shared_ptr<TcpSocket>` 改为立即 POST 线程 `shared_from_this()`；[websocket.md](../transports/websocket.md) 补协议细节（掩码帧/握手校验/ping-pong/24h 读空闲/30s 写超时）——[transport.md](../modules/transport.md)、[stdio.md](../transports/stdio.md)、[sse.md](../transports/sse.md)、[concurrency.md](../concepts/concurrency.md)
- **HTTP/存储修正**：错误体分层（传输层 JSON-RPC，服务层自身 400/413/503 为空体）；SSE `id:` 仅非 stateless；GET 流承载通知**与请求**（elicit 经 `BroadcastSse`）；202 语义更正（确认客户端发来的通知/响应）；413 为服务层 `Content-Length` 先行拦截、传输层 4MiB 为兜底；版本头**只认 initialize 的 POST 响应**（GET 流不学习）；GET 流超 8MB 仅记 Error（`NotifyError` 属 POST SSE 路径）；会话头从首个非 initialize POST 起携带；Close 双平台机制补注；高信号补充（空 `data:` 块静默忽略、退避不解析服务端 `retry:`、`SleepInterruptibly` 谓词 `running.load()` 可被 Close 打断）——[http.md](../modules/http.md)、[http-server.md](../classes/http-server.md)、[streamable-http.md](../transports/streamable-http.md)
- **概念/工程修正**：discover 声明列表为空/无交集回退 `initialize`（仅未声明保留探测版本）；`SetNegotiatedProtocolVersion` 为 `shared_mutex` 内整体交换（非"原子交换 shared_ptr"）；2025-only 通知 4→**9 种**；MRTR 行号与 `InputRequests` 字段枚举补全、三件套行号修正；OAuth 补 `AuthenticateClientCredentials()`（client_credentials grant，强制 `iss` 校验）；build 页更正 `-march=native` 表述（本地构建不可跨机分发）与 OpenSSL 说明（SHA-256 恒内置、`RAND_bytes` 仅随机源）、docs.yml 解除误缩进——[version-negotiation.md](../concepts/version-negotiation.md)、[mrtr.md](../concepts/mrtr.md)、[oauth.md](../concepts/oauth.md)、[build.md](../build.md)
- **AGENTS.md 核对修正**：用例数 554→**578**（滞后于 09-14 框架增强）；集成测试超时护栏由"`std::_Exit(1)` 快速失败"更正为 `MCP_RUN_WITH_TIMEOUT` + `MarkAbandoned()`（跳过 TearDown、runner 继续）；OpenSSL 表述改为 TLS 禁用 + PKCE 随机源回退（SHA-256 恒内置）
- **未改动项（核实后保留）**：`tests/conformance/baseline.yaml` 的 `sse-retry` 条目注记"重连未携带 Last-Event-ID"与代码"已记录 id 时携带"不矛盾（场景无 id 可带，未运行 referee 无法判定，维持实测记录）；`SessionExpired(-32009)` 不在 `JsonRpcIsKnownErrorCode` 白名单，但 404 响应由 `MakeSessionExpiredError` fallback 本地构造同码错误，客户端行为正确（代码级观察，非知识库事实）
- **未解决项**：跨语言对齐声明（TS SDK v2 校验、python/rust 常量、SEP 编号、历史等价声明）无本地证据，保留原文；`logging.md` "不设钩子时与历史逐字节一致"无法从当前代码核实
