# 2026-09-17 更新日志

## 2026-09-17-03 — 死代码清除与内部去重（构建与 578 用例全量验证通过）

- **死代码清除**（净变更实测 `git diff --stat`：42 个文件，+140/−532，净 −392 行；含本轮全部代码与文档改动）：
  - `include/mcp/protocol/McpSession.hpp` 整文件删除——被 `McpSessionHandler` 替代的旧抽象会话路径；`kDefaultRequestTimeout` 常量随之迁至 [McpSessionHandler.hpp](../../include/mcp/protocol/McpSessionHandler.hpp)
  - PlatformIO：`OpenStandardError`、`ProcessHandle::WaitForExit`、`native_handle`×2、`CreatedProcess::stderr_pipe` 删除（`IsRunning`/`Terminate` 保留）
  - HttpServer 外壳私有 `IsRequestAllowed` 与匿名 `IsLocalhostHost` 删除（`HttpServerImpl` 的 Impl 层版本保留并继续使用）
  - `src/detail/ResponseCache.hpp` 的 `Get(key, scope)` 删除（`GetAny`/`Store`/`Invalidate` 保留）
  - `src/detail/JsonSerialize_fwd.hpp` 整文件与 `ParseJsonString`/`DumpJsonString` 转发层删除——`JsonValue::Parse/Dump` 直连底层实现
  - [JsonFields.hpp](../../src/detail/JsonFields.hpp) 删除 27 个零引用 wire 键常量（含 4 个 snake_case 遗留），常量总数 119 → 92
  - `WebSocketClient.hpp` 的 `IsRunning` 声明、`TestFakes.hpp` 的 `SetStateless`/`SendCount` 删除（`EXPECT_CALL_COUNT` 按 `Sent().size()` 断言）
- **内部去重（行为不变）**：
  - 新建 [NetIoUtil.hpp](../../src/transport/detail/net/NetIoUtil.hpp)：`kMaxLineBytes`（8KB）/`kMaxHeaderBytes`（64KB）/`ToLower`/`TrimInPlace`/`Remaining` 单一来源，HttpClient/WebSocketClient/HttpServerImpl 共用
  - `JsonValue` 的 `At` 与 const `operator[]` 统一为单一查找实现（对外行为与错误文案不变）
  - `Capabilities.cpp` extensions 拷贝提取为文件内辅助函数；`HttpServerImpl` 的 `RemoveSseClient`/`RemoveSseClientEntry` 去掉恒为 true 的 `call_on_disconnect` 形参、`AcceptLoop` 去掉未用的 `port` 形参（均为 detail 内部签名）
- **构建收敛**：`cmake/FetchDependencies.cmake` 移除 FetchContent 引入与 `MCP_OPENSSL_FOUND` 死变量（configure 恢复无第三方拉取）；tests CMake 以父级目录级 `include_directories` 取代 6 处 target 级重复；根 CMakeLists 的 Werror 清单补 `mcp-event-store-tests`，transport `UNITY_BUILD OFF` 与 client `UNITY_BUILD_UNIQUE_ID` 补注释
- **conformance spec 版本单源**：`CONFORMANCE_SPEC_VERSION` 环境变量——[conformance.yml](../../.github/workflows/conformance.yml) env 定值 `2025-11-25`，[run-conformance.sh](../../scripts/run-conformance.sh) 同名 env 默认兜底同值
- **docs 修复**：新建 `docs/en/advanced/logging.md` 并双侧侧边栏收录（对齐 zh 侧）；`docs/en/overview` 废弃示例改一行说明
- **验证**：全量构建成功；`ctest` 17 目标 / 578 用例 578/578 通过（用例数不变，纯减法重构无行为变化）
- **知识库同步**：本 wiki 7 页核对更新——[mcp-session-handler.md](../classes/mcp-session-handler.md)（常量归属文件）、[transport.md](../modules/transport.md)（工厂函数列表、NetIoUtil）、[http-server.md](../classes/http-server.md)（`RemoveSseClient` 单参）、[core.md](../modules/core.md)（JsonFields 92）、[tests.md](../tests.md)（替身接口、conformance 单源）、[concurrency.md](../concepts/concurrency.md)（行号漂移）；其余命中页面（changelog 历史与描述现存符号的页面）核实无需改
